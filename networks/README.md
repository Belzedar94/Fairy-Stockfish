# Network Assets

Neural-network bytes are intentionally absent from this repository.

`manifest.json` records complete identities and policy. A filename or truncated
hash is never accepted as proof of network identity. `KOTH_v1.nnue` is reserved
for a byte-identical alias of the official legacy compatibility network and is
not a selection mechanism.
